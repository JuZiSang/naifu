import Default from '../../assets/modelplaceholder.png'

import { StoryMode } from '../story/story'

export const PrefixOptions = new Map([
    ['vanilla', { label: 'No Module', image: Default, mode: 0 as StoryMode, description: '' }],
])
